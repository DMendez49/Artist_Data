
<%= form_with(model: artist, local: true, class: "ui form") do |f| %>
<link rel="stylesheet" href='application.css'>
<div class="fieldy">
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Artis Name:" %>
  </div>
  </div>
  </div>

  <div class="field">
    <%= f.label :genra %>
    <%= f.text_field :genra, required: true, placeholder: "genra" %>
  </div>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label, placeholder: "label" %>
  </div>

  <%= f.submit class: "ui button"%>
<% end %>